import env from '../app.env';

// eslint-disable-next-line @typescript-eslint/no-namespace
export namespace Paths {
  // eslint-disable-next-line @typescript-eslint/no-namespace
  export namespace External {
    export const EXAMPLE_URL = env.ENV_EXAMPLE;
  }
}
